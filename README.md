# Hardware Repair Runbook

`app.py`、`requirements.txt`、`README.md`を同じ階層へ配置します。

```bash
pip install -r requirements.txt
streamlit run app.py
```

Streamlit Community CloudではMain file pathに`app.py`を指定します。作業終了前にJSONを保存してください。
